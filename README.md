pip install fastapi uvicorn[standard] pillow torch torchvision ftfy regex tqdm git+https://github.com/openai/CLIP.git
uvicorn main:app --reload --host 0.0.0.0 --port 8000
pip install python-multipart
